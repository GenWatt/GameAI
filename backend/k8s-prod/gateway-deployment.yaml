apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-api
  namespace: gameai-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway-api
  template:
    metadata:
      labels:
        app: gateway-api
    spec:
      containers:
      - name: gateway-api
        image: genwatt/gateway-api:latest
        ports:
        - containerPort: 5000
        envFrom:
        - configMapRef:
            name: gameai-config-prod
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-api
  namespace: gameai-prod
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: gateway-api